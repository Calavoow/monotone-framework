import parser.AST._

object Flow {

	var counter = -1
	def labelNodes(node: AstNode) : AstNode = {
		println(node.children)
		node
	}
//
//
//	def flowGraph(ast: astNode): List[(Int, Int)] = {
//
//	}

}
